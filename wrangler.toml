# Cloudflare Workers 配置
# 部署: wrangler deploy
# 静态资源在 public/ (Assets 托管), API 代理在 worker.js

name = "paper-radar"
main = "worker.js"
compatibility_date = "2024-11-25"

[assets]
directory = "./public"
not_found_handling = "single-page-application"
